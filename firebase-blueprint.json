{
  "entities": {
    "User": {
      "title": "User",
      "description": "Represents a teacher or student in the system.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string", "enum": ["Teacher", "Collaborator"] },
        "photoURL": { "type": "string" }
      },
      "required": ["uid", "name", "email", "role"]
    },
    "Project": {
      "title": "Project",
      "description": "A group of sprints with a configurable catalog of sprint states.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "sprintStates": {
          "type": "array",
          "items": { "$ref": "#/entities/SprintState" }
        },
        "createdBy": { "type": "string" }
      },
      "required": ["name", "createdBy"]
    },
    "SprintState": {
      "title": "SprintState",
      "description": "A project-level state that can be assigned to a sprint.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "color": { "type": "string" },
        "order": { "type": "number" }
      },
      "required": ["id", "name", "order"]
    },
    "Sprint": {
      "title": "Sprint",
      "description": "A container for tasks within a period.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "startDate": { "type": "string", "format": "date-time" },
        "endDate": { "type": "string", "format": "date-time" },
        "isActive": { "type": "boolean" },
        "stateId": { "type": "string" },
        "createdBy": { "type": "string" }
      },
      "required": ["name", "createdBy"]
    },
    "Task": {
      "title": "Task",
      "description": "A single piece of work.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "sprintId": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "weight": { "type": "number" },
        "status": { "type": "string", "enum": ["backlog", "todo", "in_progress", "done"] },
        "assignedTo": { "type": "string" },
        "finishedBy": { "type": "string" },
        "createdBy": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["name", "sprintId", "status", "weight", "createdBy"]
    },
    "Notification": {
      "title": "Notification",
      "description": "Push notification for task changes.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "message": { "type": "string" },
        "read": { "type": "boolean" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "message", "read", "createdAt"]
    }
  },
  "firestore": {
    "users": {
      "schema": "User",
      "description": "Global users collection"
    },
    "projects": {
      "schema": "Project",
      "description": "Global projects collection"
    },
    "sprints": {
      "schema": "Sprint",
      "description": "Global sprints collection"
    },
    "tasks": {
      "schema": "Task",
      "description": "Tasks associated with sprints"
    },
    "notifications": {
      "schema": "Notification",
      "description": "User specific notifications"
    }
  }
}
